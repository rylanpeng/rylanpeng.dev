{"config":{"lang":["en"],"separator":"[\\s\\-]+","pipeline":["stopWordFilter"],"fields":{"title":{"boost":1000.0},"text":{"boost":1.0},"tags":{"boost":1000000.0}}},"docs":[{"location":"","title":"Latest Posts","text":"<p>Welcome to my technical blog. Here I document my journey through Robotics, AI, and Embedded Systems.</p>"},{"location":"about/","title":"About","text":"<p>Welcome to my technical blog! I'm Rylan Peng, a developer interested in Robotics, Embedded Systems, and AI.</p> <p>This blog is a place where I share my deep dives into technical topics, documentation of my projects, and various insights I gain while working with hardware and software.</p> <p>Feel free to connect with me on GitHub or LinkedIn.</p>"},{"location":"1-what-is-initrd/","title":"What is initrd?","text":"<p>If you have ever peeked at <code>/boot/extlinux/extlinux.conf</code>, you probably noticed a line called <code>INITRD</code>. Have you ever wondered what this thing is, what it does, and why we need it?</p> <p>Note</p> <p>These notes are based on NVIDIA Jetson Linux R36.5 behavior and the <code>nvidia-l4t-initrd</code> package.</p>"},{"location":"1-what-is-initrd/#why-do-we-need-initrd","title":"Why do we need initrd?","text":"<p>After UEFI finishes, the kernel is launched with three key inputs:</p> <ul> <li><code>Image</code>: The kernel binary.</li> <li><code>FDT</code>: The device tree blob (DTB), which describes the hardware.</li> <li><code>INITRD</code>: &lt;--- This is what we're going to talk about today!</li> </ul> <p>You can see them in <code>extlinux.conf</code>:</p> <pre><code>LABEL primary\n    LINUX /boot/Image\n    FDT /boot/dtb/kernel_tegra234-p3768-0000+p3767-0005-nv.dtb\n    INITRD /boot/initrd\n    APPEND ${cbootargs} root=PARTUUID=&lt;...&gt; rw rootwait rootfstype=ext4\n</code></pre> <p>Even with <code>Image</code> and <code>FDT</code>, the kernel needs to know \"how\" to mount the root partition (e.g., NVMe, PCIe, ext4). </p> <p>Who knows the \"how\"? The drivers!  But where are the drivers? They are usually compiled as modules (<code>.ko</code> files), which live on the root partition itself!</p> <p>See this chicken-and-egg problem? This is why initrd exists. </p> <p>The initrd breaks the cycle: the bootloader loads it into RAM, so the kernel can access the modules before it mounts the disk.</p> <p>Note</p> <p>In one sentence: The initrd (initial ramdisk) is a small, compressed filesystem loaded into RAM by the bootloader. It provides the kernel with the drivers it needs to access the real root filesystem.</p>"},{"location":"1-what-is-initrd/#what-is-inside-initrd","title":"What is inside initrd?","text":"<p>On Jetson, <code>/boot/initrd</code> is a gzipped <code>cpio</code> archive. The files included in the initrd are specified via configuration lists in:</p> <pre><code>/etc/nv-update-initrd/list.d/\n</code></pre> <p>The <code>modules</code> list includes both in-tree and out-of-tree (OOT) modules:</p> <pre><code># nvidia-l4t-initrd deb: /etc/nv-update-initrd/list.d/modules\n\n# --- Out-of-Tree (OOT) Modules (from /lib/modules/&lt;VER&gt;/updates/) ---\nnvethernet.ko        # NVIDIA ethernet driver\nnvpps.ko             # Pulse-per-second driver\nr8126.ko             # Realtek 8126 ethernet\nr8168.ko             # Realtek 8168 ethernet\nfusb301.ko           # USB Type-C controller\n\n# --- In-Tree Modules (from /lib/modules/&lt;VER&gt;/kernel/) ---\nnvme.ko              # NVMe host driver\nnvme-core.ko         # NVMe core\ntegra-bpmp-thermal.ko # Thermal management\npwm-tegra.ko         # PWM controller (for fan)\npwm-fan.ko           # Fan control\npcie-tegra194.ko     # PCIe controller\nphy-tegra194-p2u.ko  # PCIe PHY\ntegra-xudc.ko        # USB device controller\ntypec_ucsi.ko        # USB Type-C UCSI\nucsi_ccg.ko          # USB Type-C CCG\ntypec.ko             # USB Type-C subsystem\nuas.ko               # USB Attached SCSI\n</code></pre> <p>The <code>binlist</code> includes binaries for disk encryption (LUKS) and secure storage:</p> <pre><code># nvidia-l4t-initrd deb: /etc/nv-update-initrd/list.d/binlist\n\n/usr/sbin/nvluks-srv-app    # Disk Encryption\n/usr/sbin/tee-supplicant    # Secure Storage\n/usr/lib/libteec.so.2       # TEE client library\n</code></pre> <p>Important</p> <p>Now that we know what's inside, it's clear that the initrd must stay in sync with our modules. If we ever modify or update modules (either in-tree or OOT), we better update the initrd as well!</p>"},{"location":"1-what-is-initrd/#how-to-update-initrd","title":"How to update initrd?","text":"<p>We use the <code>nv-update-initrd</code> command:</p> <pre><code>$ sudo nv-update-initrd\n</code></pre> <p>Let's dive into what <code>nv-update-initrd</code> actually does.</p> <p>The script lives at <code>/usr/sbin/nv-update-initrd</code> on the Jetson, installed by the <code>nvidia-l4t-initrd</code> Debian package. Its job is to repack the <code>/boot/initrd</code> file with fresh modules that match our current kernel.</p> <p>Here is exactly what happens when we run <code>sudo nv-update-initrd</code>, step by step:</p>"},{"location":"1-what-is-initrd/#step-1-backup-the-current-initrd","title":"Step 1: Backup the current initrd","text":"<p><pre><code>cp \"${INITRD}\" \"${INITRD_BAK}\"\n</code></pre> Before making any changes, the script clones the current working initrd file. If the update fails mid-way, we have a \"last known good\" version to restore.</p>"},{"location":"1-what-is-initrd/#step-2-unpack-the-initrd","title":"Step 2: Unpack the initrd","text":"<p><pre><code>DESTDIR=$(mktemp -d)\npushd \"${DESTDIR}\"\ngunzip -c \"${_initrd_path}\" | cpio -i --quiet\n</code></pre> The initrd is essentially a compressed archive (a <code>.cpio.gz</code> file). The script creates a temporary folder and unpacks everything there so it can modify the files. After this step, <code>$DESTDIR</code> looks like a mini Linux filesystem with <code>lib/</code>, <code>etc/</code>, <code>usr/</code>, etc.</p>"},{"location":"1-what-is-initrd/#step-3-remove-old-modules","title":"Step 3: Remove old modules","text":"<p><pre><code>rm -rf \"${DESTDIR}/lib/modules/\"*\n</code></pre> Kernel modules take up significant space. To prevent the initrd from growing uncontrollably with every update, the script wipes out all old drivers before adding fresh ones.</p>"},{"location":"1-what-is-initrd/#step-4-identify-the-kernel-version","title":"Step 4: Identify the kernel version","text":"<p><pre><code>_kernel_version=\"$(strings \"${_image_path}\" \\\n    | grep -oE \"Linux version [0-9a-zA-Z\\.\\-]+[+]* \" \\\n    | cut -d\\  -f 3 | head -1)\"\n</code></pre> The script looks at the actual kernel binary (<code>/boot/Image</code>) and extracts the version string (e.g., <code>5.15.185-tegra</code>). It needs this exact string to know which folder under <code>/lib/modules/</code> to pull the new drivers from.</p>"},{"location":"1-what-is-initrd/#step-5-inject-new-drivers","title":"Step 5: Inject new drivers","text":"<p><pre><code># Read the list of \"mission-critical\" modules\nfor file in \"${LIST_DIR}/\"*; do\n    cat \"${file}\" &gt;&gt; \"${temp_list}\"\ndone\n\n# Copy each listed module into the initrd workspace\ncopy_files_initrd \"${DESTDIR}\" \"${temp_list}\" \"${_kernel_version}\"\n</code></pre> It reads the configuration files in <code>/etc/nv-update-initrd/list.d/</code> (the <code>modules</code> and <code>binlist</code> files shown previously) to decide which drivers are mission-critical for booting. Then it pulls those fresh <code>.ko</code> files from our system's <code>/lib/modules/&lt;version&gt;/</code> into the temporary folder.</p>"},{"location":"1-what-is-initrd/#step-6-copy-modprobe-configs","title":"Step 6: Copy modprobe configs","text":"<p><pre><code>for modprobe_dir in \"/etc/modprobe.d\" \"/lib/modprobe.d\"; do\n    rm -rf \"${DESTDIR:?}/${modprobe_dir}\"\n    mkdir -p \"${DESTDIR}/${modprobe_dir}\"\n    for file in \"${modprobe_dir}\"/*.conf; do\n        copy_file config \"$file\"\n    done\ndone\n</code></pre> It copies our custom hardware rules (<code>modprobe</code> configs). These files tell the system how to load certain drivers (e.g., specific parameters for the GPU or Wi-Fi).</p>"},{"location":"1-what-is-initrd/#step-7-repacking-the-archive","title":"Step 7: Repacking the archive","text":"<p><pre><code>find . | cpio -H newc -o --quiet | gzip -9 -n &gt; \"${_initrd_path}\"\n</code></pre> Now that the files are updated, the script zips the folder back up into the <code>cpio.gz</code> format.</p>"},{"location":"1-what-is-initrd/#step-8-the-final-swap","title":"Step 8: The final swap","text":"<pre><code># Recall from Step 1: INITRD_BAK is the file we've been modifying\ncp \"${INITRD_BAK}\" \"${INITRD}\"\nrm -f \"${INITRD_BAK}\"\n</code></pre>"},{"location":"1-what-is-initrd/#conclusion","title":"Conclusion","text":"<ol> <li> <p>Booting: At the beginning of the boot process, the bootloader (UEFI) reads <code>extlinux.conf</code>, sees the <code>INITRD /boot/initrd</code> line, and copies the entire initrd file into RAM. Now the kernel has a temporary filesystem in memory containing all those critical <code>.ko</code> files and modprobe configs before the SSD is even accessible.</p> </li> <li> <p>The kernel uses the initrd: It unpacks the ramdisk, loads the NVMe/PCIe drivers from it, and uses those drivers to mount the real root filesystem on the SSD.</p> </li> <li> <p>The rest loads from disk: Once the SSD is mounted, <code>systemd</code> takes over and loads all the remaining modules (WiFi, Bluetooth, camera, sound, etc.) from <code>/lib/modules/&lt;version&gt;/</code> on the real filesystem.</p> </li> </ol> <p>That's it! That is the initrd. I got so excited after understanding how initrd works, and I hope you do as well!</p>"},{"location":"1-get-ready/","title":"Get Ready!","text":"<p>To start the Jetson Orin Nano journey, the first step is to figure out where to grab the source code and which build tools are needed.</p> <p>Note</p> <p>These notes are based on the official NVIDIA Jetson Linux Developer Guide R36.5.</p>"},{"location":"1-get-ready/#check-the-l4t-version","title":"Check the L4T Version","text":"<p>Verify the L4T (Linux for Tegra) version running on the device. In my case, it is r36.5.</p> <p>The version can be checked using the following command:</p> <pre><code>rylan-nano@ubuntu:~$ cat /etc/nv_tegra_release \n# R36 (release), REVISION: 5.0, GCID: 43688277, BOARD: generic, EABI: aarch64, DATE: Fri Jan 16 03:50:45 UTC 2026\n# KERNEL_VARIANT: oot\nTARGET_USERSPACE_LIB_DIR=nvidia\nTARGET_USERSPACE_LIB_DIR_PATH=usr/lib/aarch64-linux-gnu/nvidia\n</code></pre>"},{"location":"1-get-ready/#download-source-code","title":"Download Source Code","text":""},{"location":"1-get-ready/#1-driver-package-bsp","title":"1. Driver Package (BSP)","text":"<ol> <li>Navigate to the Jetson Linux Archive.</li> <li>Select the version corresponding to the L4T release (e.g., 36.5 - Direct Link).</li> <li>Download the Driver Package (BSP) from the DRIVERS section.</li> </ol> <p>Extract the downloaded file <code>Jetson_Linux_&lt;version&gt;_aarch64.tbz2</code> and you'll get the <code>Linux_for_Tegra</code> folder.</p>"},{"location":"1-get-ready/#2-bsp-sources","title":"2. BSP Sources","text":"<ol> <li>On the same download page, locate the SOURCES section.</li> <li>Download the Driver Package (BSP) Sources (<code>public_sources.tbz2</code>).</li> </ol>"},{"location":"1-get-ready/#extraction-procedure","title":"Extraction Procedure","text":"<p>Use the following commands to set up the source tree. Replace <code>&lt;install-path&gt;</code> with the target directory.</p> <pre><code># Extract public sources\n$ tar xf public_sources.tbz2 -C &lt;install-path&gt;/Linux_for_Tegra/..\n\n# Navigate to the source directory\n$ cd &lt;install-path&gt;/Linux_for_Tegra/source\n\n# Extract kernel and module component sources\n$ tar xf kernel_src.tbz2\n$ tar xf kernel_oot_modules_src.tbz2\n$ tar xf nvidia_kernel_display_driver_source.tbz2\n</code></pre>"},{"location":"1-get-ready/#install-build-dependencies","title":"Install Build Dependencies","text":"<pre><code>$ sudo apt-get update\n$ sudo apt-get upgrade -y\n\n# Essential tools and libraries\n$ sudo apt install -y build-essential bc flex bison libssl-dev zstd\n</code></pre>"},{"location":"2-enable-preempt-rt/","title":"Enable PREEMPT_RT","text":"<p>This guide covers how to enable PREEMPT_RT on the Jetson Orin Nano, transforming it into a real-time operating system (RTOS) ready for low-latency robotics applications.</p> <p>Note</p> <p>These notes are based on the official NVIDIA Jetson Linux Developer Guide R36.5 - Kernel Customization.</p> <p>Watch the following video for a complete walkthrough of the process: [YouTube Video Link] (xxx)</p>"},{"location":"2-enable-preempt-rt/#1-verify-system-performance","title":"1. Verify system performance","text":"<p>Before we jump into the build, let's establish a performance baseline. This helps us see the actual real-time improvement later on.</p>"},{"location":"2-enable-preempt-rt/#check-the-current-kernel","title":"Check the current kernel","text":"<p>First, let's verify the kernel version and variant currently running on the device.</p> <pre><code>$ uname -a\n# Linux ubuntu 5.15.136-tegra #1 SMP PREEMPT Tue Jan 13 01:23:45 UTC 2026 aarch64 aarch64 aarch64 GNU/Linux\n</code></pre>"},{"location":"2-enable-preempt-rt/#install-stress-test-tools","title":"Install stress-test tools","text":"<p>Install the necessary utilities for real-time performance measurement and system stressing.</p> <pre><code>$ sudo apt update\n$ sudo apt upgrade -y\n\n# Install RT and stress testing tools\n$ sudo apt install -y rt-tests stress-ng\n</code></pre>"},{"location":"2-enable-preempt-rt/#run-latency-tests","title":"Run latency tests","text":"<p>To measure the baseline latency, run a stress test in one terminal and the cyclic test in another.</p> <ol> <li> <p>Terminal 1: Start the CPU and I/O stress:     <pre><code>$ sudo stress-ng --cpu 8 --io 4 --vm 2 --vm-bytes 1G --timeout 30s\n</code></pre></p> </li> <li> <p>Terminal 2: Run the latency measurement:     <pre><code>$ sudo cyclictest --mlockall --smp --priority=80 --interval=200 --duration=10m\n</code></pre></p> </li> </ol> <p>Example Output</p> <p></p>"},{"location":"2-enable-preempt-rt/#2-prerequisites","title":"2. Prerequisites","text":"<p>Make sure to have the source code and packages ready before moving forward. Check out Get Ready!</p>"},{"location":"2-enable-preempt-rt/#3-build-and-enable-preempt_rt","title":"3. Build and enable PREEMPT_RT","text":"<p>Now for the fun part --&gt; building and enabling the patch! We'll use a helper repository to streamline the process.</p> <p>Download the helper repository and run the script.</p>"},{"location":"2-enable-preempt-rt/#clone-the-helper-repository","title":"Clone the helper repository","text":"<pre><code>$ git clone git@github.com:rylanpeng/jetson-orin-nano-preempt-rt.git\n</code></pre>"},{"location":"2-enable-preempt-rt/#run-the-build-script","title":"Run the build script","text":"<pre><code>$ cd jetson-orin-nano-preempt-rt\n$ ./build-preempt-rt.sh &lt;install-path&gt;\n</code></pre> <p>Note</p> <p>The <code>&lt;install-path&gt;</code> should point to the root directory where the source code was extracted. The script will look for files within <code>&lt;install-path&gt;/Linux_for_Tegra/source/</code>.</p>"},{"location":"2-enable-preempt-rt/#4-verify-the-installation","title":"4. Verify the installation","text":"<p>Once the script finishes, it's a good idea to double-check that everything was installed correctly.</p>"},{"location":"2-enable-preempt-rt/#inspect-the-boot-directory","title":"Inspect the /boot directory","text":"<p>We should see <code>Image.backup</code> and <code>initrd.backup</code> files created alongside the new kernel image.</p> <pre><code>$ ls /boot\n# Image  Image.backup  initrd  initrd.backup  ...\n</code></pre>"},{"location":"2-enable-preempt-rt/#inspect-kernel-modules","title":"Inspect kernel modules","text":"<p>Check <code>/lib/modules</code> to ensure both the standard and RT versions of the kernel modules are present.</p> <pre><code>$ ls /lib/modules\n# 5.15.136-tegra  5.15.136-rt-tegra\n</code></pre>"},{"location":"2-enable-preempt-rt/#5-add-a-fallback-boot-entry","title":"5. Add a fallback boot entry","text":""},{"location":"2-enable-preempt-rt/#edit-extlinuxconf","title":"Edit extlinux.conf","text":"<p>Open the boot configuration file with <code>vim</code> or preferred text editor.</p> <pre><code>$ sudo vim /boot/extlinux/extlinux.conf\n</code></pre>"},{"location":"2-enable-preempt-rt/#add-the-backup-entry","title":"Add the backup entry","text":"<p>Modify the configuration to include a fallback option:</p> <ol> <li>Uncomment the <code>backup</code> section.</li> <li>Copy the <code>APPEND</code> line from the current default section to the <code>backup</code> section.</li> <li>Update the <code>INITRD</code> path to point to <code>/boot/initrd.backup</code>.</li> </ol> <p>Example Configuration</p> <pre><code>LABEL backup\n    MENU LABEL backup kernel\n    LINUX /boot/Image.backup\n    INITRD /boot/initrd.backup\n    APPEND ${cbootargs} root=PARTUUID=cb24a353-d890-4252-9751-21d939088c7c rw rootwait rootfstype=ext4 mminit_loglevel=4 console=ttyTCU0,115200 firmware_class.path=/etc/firmware fbcon=map:0 video=efifb:off console=tty0 efi=runtime pci=pcie_bus_perf nvme.use_threaded_interrupts=1 nv-auto-config\n</code></pre>"},{"location":"2-enable-preempt-rt/#6-reboot","title":"6. Reboot","text":"<pre><code>$ sudo reboot\n</code></pre>"},{"location":"2-enable-preempt-rt/#7-verify-the-boot-selection","title":"7. Verify the boot selection","text":"<p>During the boot process, we will be presented with the boot menu.</p> <p>Select the backup entry if you ever need to roll back to the original kernel; otherwise, the system will boot using the new PREEMPT_RT image by default.</p>"},{"location":"2-enable-preempt-rt/#8-verify-the-results","title":"8. Verify the results","text":""},{"location":"2-enable-preempt-rt/#check-the-rt-kernel-variant","title":"Check the RT kernel variant","text":"<p>Run <code>uname -a</code> again, and you should see the <code>PREEMPT_RT</code> identifier!</p> <pre><code>$ uname -a\n# Linux ubuntu 5.15.136-rt-tegra #1 SMP PREEMPT_RT Tue Jan 13 01:23:45 UTC 2026 aarch64 aarch64 aarch64 GNU/Linux\n</code></pre>"},{"location":"2-enable-preempt-rt/#re-run-latency-tests","title":"Re-run latency tests","text":"<p>Perform the stress test and <code>cyclictest</code> again to observe the improvement in latency.</p> <p>Example Output</p> <p></p>"},{"location":"2-enable-preempt-rt/#optimizing-performance-optional","title":"Optimizing performance (Optional)","text":"<p>You can boost performance even further by increasing the kernel timer frequency (<code>CONFIG_HZ</code>).</p>"},{"location":"2-enable-preempt-rt/#1-open-defconfig","title":"1. Open defconfig","text":"<pre><code>$ vim Linux_for_Tegra/source/kernel/kernel-jammy-src/arch/arm64/configs/defconfig\n</code></pre>"},{"location":"2-enable-preempt-rt/#2-update-configuration","title":"2. Update configuration","text":"<pre><code>CONFIG_HZ_1000=y\n</code></pre>"},{"location":"2-enable-preempt-rt/#3-build-and-install-again","title":"3. Build and install again","text":""},{"location":"2-enable-preempt-rt/#restore-original-images","title":"Restore original images","text":"<p>Restore the original <code>Image</code> and <code>initrd</code> before rebuilding. This ensures the script's backup step saves the non-RT kernel as the fallback, not the RT kernel currently in <code>/boot</code>.</p> <pre><code>$ sudo cp /boot/Image.backup /boot/Image\n$ sudo cp /boot/initrd.backup /boot/initrd\n</code></pre>"},{"location":"2-enable-preempt-rt/#re-run-the-build-script","title":"Re-run the build script","text":"<pre><code>$ ./build-preempt-rt.sh &lt;install-path&gt;\n</code></pre>"},{"location":"2-enable-preempt-rt/#5-reboot","title":"5. Reboot","text":"<p>Finally, reboot the system to apply the changes.</p> <pre><code>sudo reboot\n</code></pre>"},{"location":"3-pytorch/","title":"PyTorch Setup on Jetson Orin Nano","text":"<p>The Jetson Orin Nano uses an ARM64 integrated GPU (nvgpu) with a custom L4T CUDA stack, so standard PyPI <code>torch</code> wheels won't work. This post walks through getting PyTorch running with proper GPU support.</p> <p>Note</p> <p>The steps below reflect my specific setup: JetPack 6.2, L4T 36.5, CUDA 12.6. Some tweaks may be needed for other versions if following this blog.</p> <p>References</p> <ul> <li>PyTorch for Jetson \u2013 NVIDIA Developer Forums</li> <li>Install PyTorch on Jetson Platform</li> <li>PyTorch Jetson Release Notes</li> </ul>"},{"location":"3-pytorch/#1-check-version","title":"1. Check version","text":"<p>First, check the JetPack / L4T / CUDA version running on the device.</p> <pre><code>$ cat /etc/nv_tegra_release\n# R36 (release), REVISION: 5.0\n\n$ nvidia-smi\n| NVIDIA-SMI 540.5.0    Driver Version: 540.5.0    CUDA Version: 12.6 |\n</code></pre>"},{"location":"3-pytorch/#2-find-the-right-wheel","title":"2. Find the right wheel","text":"<p>NVIDIA hosts Jetson-specific PyTorch wheels at:</p> <pre><code>https://developer.download.nvidia.com/compute/redist/jp/\n</code></pre> <p>Browse that URL to find the directory closest to your JetPack version. In my case, I'm on JetPack 6.2, so I'd expect a <code>v62</code>, but listing the available directories showed:</p> <pre><code>$ curl -s https://developer.download.nvidia.com/compute/redist/jp/\n# Lists: v33, v40, ..., v60, v61\n</code></pre> <p>No <code>v62</code>, so I went with <code>v61</code> as the closest match. Then I checked what wheel is available inside:</p> <pre><code>$ curl -s https://developer.download.nvidia.com/compute/redist/jp/v61/pytorch/\n# torch-2.5.0a0+872d972e41.nv24.08.17622132-cp310-cp310-linux_aarch64.whl\n</code></pre> <p>So the wheel I'll use is <code>torch-2.5.0a0+872d972e41.nv24.08.17622132-cp310-cp310-linux_aarch64.whl</code>.</p>"},{"location":"3-pytorch/#3-install-system-packages","title":"3. Install system packages","text":"<pre><code>$ sudo apt-get -y update\n$ sudo apt-get install -y python3-pip libopenblas-dev\n</code></pre>"},{"location":"3-pytorch/#4-install-cusparselt","title":"4. Install cusparselt","text":"<p>Required for any PyTorch wheel from the 24.06 release onward.</p> <p>Note</p> <p>The <code>install_cusparselt.sh</code> script only supports CUDA 12.1\u201312.4. Since our device has CUDA 12.6, passing <code>CUDA_VERSION=12.4</code> tricks the script into the correct branch.</p> <pre><code>$ wget https://raw.githubusercontent.com/pytorch/pytorch/5c6af2b583709f6176898c017424dc9981023c28/.ci/docker/common/install_cusparselt.sh\n\n$ sudo CUDA_VERSION=12.4 bash ./install_cusparselt.sh\n</code></pre>"},{"location":"3-pytorch/#5-configure-uv-to-use-the-nvidia-wheel","title":"5. Configure uv to use the NVIDIA wheel","text":"<p>By default, <code>uv sync</code> pulls <code>torch</code> from PyPI on every run, which would overwrite the Jetson wheel with an x86_64 incompatible one. Fix this by pinning <code>torch</code> to the NVIDIA wheel URL in <code>pyproject.toml</code>.</p> <p>First, initialize uv if you haven't already:</p> <pre><code>$ uv init\n</code></pre> <p>Then add the following to <code>pyproject.toml</code>:</p> <pre><code>[tool.uv.sources]\ntorch = { url = \"https://developer.download.nvidia.com/compute/redist/jp/v61/pytorch/torch-2.5.0a0+872d972e41.nv24.08.17622132-cp310-cp310-linux_aarch64.whl\" }\n</code></pre> <p>Also pin NumPy to <code>&lt;2</code>, since torch 2.5 was compiled against NumPy 1.x and will break with NumPy 2.x:</p> <pre><code>[project]\nname = \"my-project\"\nversion = \"0.1.0\"\nrequires-python = \"&gt;=3.10\"\ndependencies = [\n    \"torch\",\n    \"numpy&gt;=1.26.4,&lt;2\",\n]\n\n[tool.uv.sources]\ntorch = { url = \"https://developer.download.nvidia.com/compute/redist/jp/v61/pytorch/torch-2.5.0a0+872d972e41.nv24.08.17622132-cp310-cp310-linux_aarch64.whl\" }\n</code></pre>"},{"location":"3-pytorch/#6-set-uv_skip_wheel_filename_check1","title":"6. Set <code>UV_SKIP_WHEEL_FILENAME_CHECK=1</code>","text":"<p><code>uv</code> strictly validates that the version string inside the wheel metadata matches the wheel filename. NVIDIA's wheel has an inconsistent version string (<code>nv24.8</code> in metadata vs <code>nv24.08.17622132</code> derived from the filename), which causes:</p> <pre><code>error: Failed to install: torch-...\n  Caused by: Wheel version does not match filename (2.5.0a0+872d972e41.nv24.8 != 2.5.0a0+872d972e41.nv24.8.17622132)\n</code></pre> <p>Persist the bypass in <code>~/.bashrc</code> so you don't have to set it every time:</p> <pre><code>$ echo 'export UV_SKIP_WHEEL_FILENAME_CHECK=1' &gt;&gt; ~/.bashrc\n$ source ~/.bashrc\n</code></pre> <p>Then run:</p> <pre><code>$ uv sync\n</code></pre>"},{"location":"3-pytorch/#7-verify-gpu-is-working","title":"7. Verify GPU is working","text":"<pre><code>$ uv run python -c \"import torch; print(torch.__version__); print('CUDA available:', torch.cuda.is_available()); print('Device count:', torch.cuda.device_count())\"\n\n2.5.0a0+872d972e41.nv24.08\nCUDA available: True\nDevice count: 1\n</code></pre>"},{"location":"3-pytorch/#troubleshooting","title":"Troubleshooting","text":"<p>A few extra errors I ran into. May not apply to your setup, but leaving these here just in case.</p>"},{"location":"3-pytorch/#1-libnvtoolsextso1","title":"1. libnvToolsExt.so.1","text":"<pre><code>OSError: libnvToolsExt.so.1: cannot open shared object file: No such file or directory\n</code></pre> <p>Fix: <pre><code>$ sudo apt-get install -y libnvtoolsext1 cuda-nvtx-12-6\n</code></pre></p>"},{"location":"3-pytorch/#2-libcuptiso12","title":"2. libcupti.so.12","text":"<pre><code>ImportError: libcupti.so.12: cannot open shared object file: No such file or directory\n</code></pre> <p>Fix: <pre><code>$ sudo apt-get install -y cuda-cupti-12-6\n</code></pre></p>"},{"location":"3-pytorch/#example","title":"Example","text":"<p>A minimal working example can be found here: rylanpeng/jetson-orin-nano-pytorch-setup</p>"},{"location":"4-genesis-rl/","title":"Bipedal Robot RL Simulation on Jetson Orin Nano","text":"<p>Training a two-legged robot to walk using PPO and the Genesis physics simulator, running entirely on the Jetson Orin Nano.</p> <p>Note</p> <p>This post walks through rylanpeng/jetson-orin-nano-two-leg-robot-simulation, built with the help of Copilot. PyTorch must already be installed on the device (see 3-pytorch.md if you haven't done that yet).</p> <p>References</p> <ul> <li>Genesis: Embodied AI Physics Simulator</li> <li>Genesis locomotion examples</li> <li>rsl_rl: Legged Robotics RL</li> </ul>"},{"location":"4-genesis-rl/#1-clone-the-repo","title":"1. Clone the repo","text":"<pre><code>$ git clone git@github.com:rylanpeng/jetson-orin-nano-two-leg-robot-simulation.git\n$ cd jetson-orin-nano-two-leg-robot-simulation\n</code></pre>"},{"location":"4-genesis-rl/#2-install-python-dependencies","title":"2. Install Python dependencies","text":"<pre><code>$ uv sync\n</code></pre> <p>Note</p> <p><code>uv sync</code> pulls <code>torch</code> from the NVIDIA Jetson wheel pinned in <code>pyproject.toml</code> under <code>[tool.uv.sources]</code>, so it won't accidentally install the wrong architecture. Make sure <code>UV_SKIP_WHEEL_FILENAME_CHECK=1</code> is set in your shell (see 3-pytorch.md).</p>"},{"location":"4-genesis-rl/#3-patch-genesis-for-jetson","title":"3. Patch Genesis for Jetson","text":"<p>Run this once after every <code>uv sync</code>:</p> <pre><code>$ bash patch_genesis.sh\n</code></pre> <p>Expected output:</p> <pre><code>  [+] Added 'import re' to .venv/lib/python3.10/site-packages/genesis/utils/misc.py\n  [+] Patched version parsing in .venv/lib/python3.10/site-packages/genesis/utils/misc.py\n  [+] Patched version parsing in .venv/lib/python3.10/site-packages/genesis/__init__.py\nDone.\n</code></pre> <p>If you see <code>[=] Already patched or no match</code>, the patch was already applied, nothing to do.</p> <p>Why is this needed?</p> <p>Genesis 0.4.0 parses the PyTorch version string using <code>int()</code>:</p> <pre><code>tuple(map(int, torch.__version__.replace(\"+\", \".\").split(\".\")[:3]))\n</code></pre> <p>On Jetson, <code>torch.__version__</code> is <code>2.5.0a0+872d972e41.nv24.08</code>. After the replace and split, the first three elements are <code>['2', '5', '0a0']</code>, and <code>int('0a0')</code> raises a <code>ValueError</code>.</p> <p>The patch replaces these calls with a regex that extracts only the numeric parts:</p> <pre><code>tuple(int(x) for x in re.split(r\"[^0-9]+\", torch.__version__) if x)[:3]\n</code></pre>"},{"location":"4-genesis-rl/#4-train","title":"4. Train","text":"<p>Default training runs 500 iterations and saves checkpoints every 100.</p> <pre><code>$ uv run python -m two_leg_robot.train\n</code></pre> <p>Logs and checkpoints are saved under <code>logs/exp_&lt;timestamp&gt;/</code>. A <code>logs/latest</code> symlink always points to the most recent run.</p> <p>You can customize the run:</p> <pre><code># Custom log prefix and longer run\n$ uv run python -m two_leg_robot.train --log_dir my_experiment --iterations 1000\n</code></pre> <p>The console prints a table after each iteration:</p> <pre><code>################################################################################\n                           Learning iteration 200/200                            \n\n                            Total steps: 3276800 \n                       Steps per second: 10407 \n                        Collection time: 0.787s \n                          Learning time: 0.787s \n                        Mean value loss: 0.0051\n                    Mean surrogate loss: -0.0071\n                      Mean entropy loss: 10.2962\n                            Mean reward: 14.21\n                    Mean episode length: 931.53\n                  Mean action noise std: 0.88\n      Mean episode rew_tracking_lin_vel: 0.8296\n      Mean episode rew_tracking_ang_vel: 0.1431\n             Mean episode rew_lin_vel_z: -0.0481\n           Mean episode rew_base_height: -0.0091\n           Mean episode rew_action_rate: -0.1210\n    Mean episode rew_similar_to_default: -0.0662\n--------------------------------------------------------------------------------\n                         Iteration time: 1.57s\n                           Time elapsed: 00:05:29\n</code></pre> <p>Monitor training in TensorBoard:</p> <pre><code>$ uv run tensorboard --logdir logs\n</code></pre> <p>Open <code>http://localhost:6006</code> in a browser. The <code>Train/mean_reward</code> curve should trend upward as the robot learns to follow velocity commands.</p>"},{"location":"4-genesis-rl/#5-evaluate","title":"5. Evaluate","text":"<p>Evaluation loads the most recent checkpoint from <code>logs/latest</code>:</p> <pre><code>$ uv run python -m two_leg_robot.eval\n</code></pre> <p>To load a specific run and checkpoint:</p> <pre><code>$ uv run python -m two_leg_robot.eval --log_dir exp_2026-04-03_01-15-57 --model_id 500\n</code></pre> <p>The viewer opens and shows the robot walking under randomly sampled velocity commands.</p>"},{"location":"4-genesis-rl/#6-teleoperate","title":"6. Teleoperate","text":"<p>Drive the robot manually using the keyboard:</p> <pre><code>$ uv run python -m two_leg_robot.eval_teleop\n</code></pre> <p>Controls:</p> Key Action <code>w</code> / <code>s</code> Forward / backward <code>a</code> / <code>d</code> Strafe left / right <code>q</code> / <code>e</code> Yaw left / right <code>ESC</code> Exit viewer"},{"location":"4-genesis-rl/#result","title":"Result","text":""},{"location":"4-genesis-rl/#tensorboard","title":"TensorBoard","text":"<p>All training metrics are logged under <code>logs/&lt;run_name&gt;/</code> and viewable in TensorBoard:</p> <pre><code>$ uv run tensorboard --logdir logs\n</code></pre> <p>Key panels to watch:</p> <ul> <li>Train/mean_reward: the main signal. Should trend up. If it plateaus early, the reward scales or network capacity may need tuning.</li> <li>Loss/surrogate: the PPO policy gradient loss. Should decrease and stabilize.</li> <li>Loss/learning_rate: the adaptive KL controller adjusts this automatically. Large oscillations suggest unstable training.</li> <li>Policy/mean_noise_std: exploration level. Should decrease gradually as the policy converges.</li> <li>Episode/rew_*: per-reward-term breakdowns. Useful for diagnosing which terms are dominating or conflicting.</li> </ul>"},{"location":"5-ssd1306-spi/","title":"Talk to SSD1306 using SPI on Jetson Orin Nano","text":"<p>Driving a small OLED display over SPI from the Jetson Orin Nano's 40-pin header. This covers hardware wiring, device-tree overlay configuration, and a Python test script.</p> <p>References</p> <ul> <li>JetsonHacks SPI Tutorial</li> <li>Jetson Orin Nano GPIO Header Pinout</li> </ul>"},{"location":"5-ssd1306-spi/#1-hardware-setup","title":"1. Hardware Setup","text":""},{"location":"5-ssd1306-spi/#pin-connections","title":"Pin Connections","text":"<p>The SSD1306 OLED has 6 pins. Connect them to the Jetson's 40-pin header as follows:</p> OLED Pin Function Jetson Pin Header Label GND Ground Pin 6 GND VCC 3.3 V power Pin 1 3V3 SCK SPI clock Pin 23 SPI0_SCK SDA MOSI (data in) Pin 19 SPI0_MOSI RES Display reset Pin 31 GPIO DC Data / Command select Pin 32 GPIO CS Chip Select Pin 24 SPI0_CS0"},{"location":"5-ssd1306-spi/#dc-pin","title":"DC pin","text":"<p>SPI sends a raw stream of bits. The OLED needs to distinguish between commands (e.g. \"set brightness to 50%\") and pixel data (e.g. \"turn on this pixel\"). The DC pin provides that signal:</p> <ul> <li>DC LOW (0 V) \u2192 next bits are interpreted as commands</li> <li>DC HIGH (3.3 V) \u2192 next bits are interpreted as pixel data</li> </ul>"},{"location":"5-ssd1306-spi/#typical-spi-transaction","title":"Typical SPI transaction","text":"<ol> <li>Master pulls <code>CS</code> low to select the target device.</li> <li>Master toggles <code>SCLK</code> and shifts data on <code>MOSI</code>.</li> <li>Master sets <code>CS</code> high to end the transaction.</li> </ol>"},{"location":"5-ssd1306-spi/#wiring","title":"Wiring","text":""},{"location":"5-ssd1306-spi/#2-configure-the-device-tree-overlay","title":"2. Configure the Device-Tree Overlay","text":"<p>The SPI controller and GPIO pins must be activated via a device-tree overlay. <code>jetson-io.py</code> handles this without manually editing DTS files.</p>"},{"location":"5-ssd1306-spi/#21-check-the-current-boot-config","title":"2.1 Check the current boot config","text":"<p>Before making any changes, note the current state of <code>extlinux.conf</code> so we can verify what changed afterwards.</p> <p></p>"},{"location":"5-ssd1306-spi/#22-run-jetson-iopy","title":"2.2 Run jetson-io.py","text":"<pre><code>$ sudo /opt/nvidia/jetson-io/jetson-io.py\n</code></pre>"},{"location":"5-ssd1306-spi/#23-select-configure-jetson-40pin-header","title":"2.3 Select Configure Jetson 40pin Header","text":""},{"location":"5-ssd1306-spi/#24-select-configure-header-pins-manually","title":"2.4 Select Configure header pins manually","text":""},{"location":"5-ssd1306-spi/#25-set-pin-functions","title":"2.5 Set pin functions","text":"<p>Change the following assignments:</p> <ul> <li>Pin 19, 21, 23, 24, 26 \u2192 <code>spi</code></li> <li>Pin 31 \u2192 <code>gpio</code></li> <li>Pin 32 \u2192 <code>gpio</code></li> </ul> <p></p>"},{"location":"5-ssd1306-spi/#26-save-pin-changes","title":"2.6 Save pin changes","text":"<p>Select Back, then Save pin changes.</p> <p></p>"},{"location":"5-ssd1306-spi/#27-exit-without-rebooting","title":"2.7 Exit without rebooting","text":"<p>Select Save and exit without rebooting.</p> <p>Warning</p> <p>Always verify the config before rebooting. A bad overlay can prevent the system from booting, potentially requiring a full reflash.</p> <p></p>"},{"location":"5-ssd1306-spi/#3-verify-the-overlay","title":"3. Verify the Overlay","text":""},{"location":"5-ssd1306-spi/#check-extlinuxconf","title":"Check extlinux.conf","text":"<p>Open <code>/boot/extlinux/extlinux.conf</code> and confirm a new <code>JetsonIO</code> section has appeared, referencing <code>jetson-io-hdr40-user-custom.dtbo</code>.</p>"},{"location":"5-ssd1306-spi/#inspect-the-overlay-optional","title":"Inspect the overlay (optional)","text":"<p>To read the raw DTS and confirm the SPI and GPIO pins are configured correctly, decompile the overlay:</p> <pre><code># Copy the overlay to a working directory\n$ mkdir -p ~/workspace\n$ cp /boot/jetson-io-hdr40-user-custom.dtbo ~/workspace/\n\n# Install the device-tree compiler\n$ sudo apt-get install device-tree-compiler\n\n# Decompile\n$ dtc -I dtb -O dts ~/workspace/jetson-io-hdr40-user-custom.dtbo -o ~/workspace/temp.dts\n\n# Inspect\n$ cat ~/workspace/temp.dts\n</code></pre> <p>The DTS shows the SPI controller node and the two GPIO pins wired to <code>RES</code> and <code>DC</code>.</p>"},{"location":"5-ssd1306-spi/#4-reboot","title":"4. Reboot","text":"<pre><code>$ sudo reboot\n</code></pre>"},{"location":"5-ssd1306-spi/#5-run-the-python-test","title":"5. Run the Python Test","text":"<pre><code>$ git clone git@github.com:rylanpeng/jetson-orin-nano-ssd1306-spi.git\n$ cd jetson-orin-nano-ssd1306-spi\n$ uv sync\n$ uv run python display.py\n</code></pre> <p>Result</p> <p></p>"},{"location":"category/jetson-orin-nano/","title":"Jetson Orin Nano","text":""},{"location":"category/hands-on/","title":"Hands-on","text":""},{"location":"category/deep-dive/","title":"Deep Dive","text":""}]}